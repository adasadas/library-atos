You can build project and run tests by Intellij IDEA or using mvn.

mvn commands:
    compiling project:
        mvn compile
    packing project to .jar:
        mvn package
    running tests:
        mvn clean test
